const GlobalReducer = (state = {}, action) => {
    // console.log(state, action);
    // switch (action.type) {
    //     case 'SET_TITLE':
    //         state = {
    //             ...state,
    //             title: 'Dum'
    //         }
    //         break;
    // }
    return state;
};

export default GlobalReducer;